# GAS 変更点（第 5 弾）— アルバムの読み込みが詰まる問題

**適用するファイルは [`GAS_コード_適用済み.gs`](GAS_コード_適用済み.gs) です。**
この変更はすでに反映済みなので、そのファイルを丸ごと貼り替えてください。
以下は「何をなぜ変えたか」の記録です。

---

## 症状

アルバムが「読み込んでいます…」から長時間動かない。

## 実測した原因

**遅いのではなく、断続的に失敗していました。** 同じ endpoint の実測です。

| 状況 | 結果 |
|---|---|
| 落ち着いているとき | **1.7 秒**で 30 件（正常） |
| 短時間にアクセスが集中したあと | 27 秒 → **HTTP 404**（Google のエラーページ） |
| 同上 | 140 秒 / 810 秒 |

Apps Script は集中アクセスで絞られます。式当日にゲストが一斉にアルバムを開く状況が
まさにこれです。

> この数値の一部は、調査のために短時間へ集中して叩いたことが原因です。
> 現在は 1.7 秒で正常に返っています。ただし「集中すると絞られる」という
> 性質そのものは変わらないので、当日は同じことが起こりえます。

## なぜキャッシュが効いていなかったか

`getAlbum` は結果を `CacheService` に 20 秒（`ALBUM_CACHE_SEC`）置きます。
ところがキーが

```
album_v<世代>_<件数>_<pageToken のダイジェスト>
```

で、**`<世代>` は写真が 1 枚届くたびに `bumpAlbumVersion()` が進めます。**

披露宴中は撮影が途切れません。つまり**このキャッシュはほぼ一度も効かず、
ゲストの読み込みが毎回 Drive への実アクセスになっていました。**

## 変更内容

世代番号を**含まない**第 2 のキーを、短い期限で追加しました。

| | 世代つきキー（従来） | 集中をまとめるキー（新規） |
|---|---|---|
| キー | `album_v<世代>_<件数>_<token>` | `album_burst_<件数>_<token>` |
| 期限 | 20 秒（`ALBUM_CACHE_SEC`） | **10 秒**（`ALBUM_BURST_SEC`） |
| アップロードで無効化 | される | **されない** |
| 役目 | 平常時の使い回し | 同時アクセスを Drive 1 回にまとめる |

読む順番は「世代つき → 集中まとめ → Drive」で、Drive を呼んだら**両方に書きます**。

```js
if (get(cacheKey)) return ...;      // 平常時
if (get(burstKey)) return ...;      // 撮影直後でも 10 秒は使い回す
const result = listImagesViaRestApi(...);
cache.put(cacheKey, payload, CONFIG.ALBUM_CACHE_SEC);
cache.put(burstKey, payload, CONFIG.ALBUM_BURST_SEC);
```

## 効果（模擬計算）

5 分間・毎秒 2 人がアルバムを開く条件で、撮影の間隔を変えて Drive 呼び出し回数を
比べました。

| 撮影の間隔 | 変更前 | 変更後 | 削減 |
|---|---|---|---|
| 30 秒に 1 枚 | 20 回 | 20 回 | 0% |
| 20 秒に 1 枚 | 15 回 | 15 回 | 0% |
| 10 秒に 1 枚 | 30 回 | 30 回 | 0% |
| **5 秒に 1 枚** | 60 回 | 30 回 | **50%** |
| **3 秒に 1 枚** | 100 回 | 30 回 | **70%** |
| **1 秒に 1 枚** | 300 回 | 30 回 | **90%** |

撮影がゆっくりなときは変わりません（世代つきキーだけで足りているため）。
**披露宴の実際にあたる「数秒に 1 枚」で効きます。**

> **この模擬計算には限界があります。** 1 秒の中を順番に処理しているので、
> 「同時に 80 人が到達して全員がキャッシュ未書き込みの状態で Drive を呼ぶ」
> という本当の同時実行は再現できていません。実機ではその競合が起きるため、
> **実際の削減幅はこの表より大きくなります。**

## 代償

**撮った写真がアルバムに出るまで、最大 10 秒かかります。**

`ALBUM_CACHE_SEC` を 45 秒などに伸ばす方法もありますが、そちらは
「撮ったのに出てこない」時間がそのまま 45 秒に伸びます。10 秒なら
「↻ を押したら出る」と感じられる範囲に収まるため、こちらを選びました。

長すぎる／短すぎると感じたら `CONFIG.ALBUM_BURST_SEC` だけを直してください。

## 適用手順

1. Apps Script エディタを開く
2. [`GAS_コード_適用済み.gs`](GAS_コード_適用済み.gs) の中身を丸ごと貼り替える
3. **デプロイを管理 → 鉛筆マーク → 新バージョン** で更新する
   - **「新しいデプロイ」を選ばないでください。** URL が変わり、配布済みの
     QR コードが使えなくなります
4. `verifySetup()` を実行して設定を確認する

## 検証したこと

- 構文が通ること（1124 行）
- 読む順番と、Drive を呼んだあと両方のキーに書くこと
- 撮影頻度ごとの Drive 呼び出し回数（上の表）
- 一斉アクセス時に呼び出し回数がゲスト数に比例しなくなること
- 既存のパッチ（第 4 弾 A〜F）が崩れていないこと 9 項目
